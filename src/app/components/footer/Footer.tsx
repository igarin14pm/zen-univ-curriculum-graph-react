import style from './Footer.module.css';

const Footer = (): React.JSX.Element => {
  return (
    <footer className={style.footer}>
      <small>&copy; Igarin 2026</small>
    </footer>
  );
};

export default Footer;
